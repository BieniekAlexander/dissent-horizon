[project]
name = "dh-balance"
version = "0.1.0"
description = "Costed Response Analysis tooling for Dissent Horizon factions"
requires-python = ">=3.11"
dependencies = ["networkx>=3.0", "pyyaml>=6.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
